using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class SectionManager : MonoBehaviour
{
  /*  public enum sectionDifficulty {easy, medium, challenger, hard, insane, unset}
    public sectionDifficulty difficulty = sectionDifficulty.unset;
    sectionDifficulty _difficulty = sectionDifficulty.unset;

    public SpriteRenderer rightWall;
    public SpriteRenderer leftWall;

    public Sprite[] wallSprites; // ordered from 0 to 4 with 0 being easy and 4 being isane









    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        WallChanger();
    }
    
    void WallChanger()
    {
        if(difficulty != _difficulty)
        {
            switch(difficulty)
            {
                case sectionDifficulty.easy:
                    rightWall.color = Color.white;
                    leftWall.color = Color.white;
                    rightWall.sprite = wallSprites[0];
                    leftWall.sprite = wallSprites[0];
                    _difficulty = difficulty;
                    break;
                case sectionDifficulty.medium:
                    rightWall.color = Color.white;
                    leftWall.color = Color.white;
                    rightWall.sprite = wallSprites[1];
                    leftWall.sprite = wallSprites[1];
                    _difficulty = difficulty;
                    break;
                case sectionDifficulty.challenger:
                    rightWall.color = Color.white;
                    leftWall.color = Color.white;
                    rightWall.sprite = wallSprites[2];
                    leftWall.sprite = wallSprites[2];
                    _difficulty = difficulty;
                    break;
                case sectionDifficulty.hard:
                    rightWall.color = Color.white;
                    leftWall.color = Color.white;
                    rightWall.sprite = wallSprites[3];
                    leftWall.sprite = wallSprites[3];
                    _difficulty = difficulty;
                    break;
                case sectionDifficulty.insane:
                    rightWall.color = Color.white;
                    leftWall.color = Color.white;
                    rightWall.sprite = wallSprites[4];
                    leftWall.sprite = wallSprites[4];
                    _difficulty = difficulty;
                    break;
                case sectionDifficulty.unset:
                    _difficulty = difficulty;
                    Debug.Log("tile does not have asigned difficulty");
                    rightWall.color = Color.gray;
                    leftWall.color = Color.gray;
                    break;
            }
        }
    }

    */
}
